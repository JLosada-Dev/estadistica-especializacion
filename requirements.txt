# Dependencias para desplegar los dashboards en Streamlit Community Cloud.
# El paquete local `estadistica` (src/) se importa vía el bootstrap de rutas
# en cada app.py, así que no hace falta instalarlo aquí.
streamlit>=1.58
plotly>=6.8
pandas>=2.2
numpy>=2.0
scikit-learn>=1.5
mlxtend>=0.23
scipy>=1.13
openpyxl>=3.1
